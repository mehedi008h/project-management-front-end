import { Container, Graph, Widgets } from "../components";

const HomePage = () => {
    return (
        <Container>
            <Widgets />
            <Graph />
        </Container>
    );
};

export default HomePage;
