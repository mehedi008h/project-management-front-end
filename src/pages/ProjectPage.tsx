import { Container, FilterMenu, Projects } from "../components";

const ProjectPage = () => {
    return (
        <Container>
            <FilterMenu />
            <Projects />
        </Container>
    );
};

export default ProjectPage;
