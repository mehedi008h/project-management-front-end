import { Container, FilterMenu } from "../components";

const ProjectPage = () => {
    return (
        <Container>
            <FilterMenu />
        </Container>
    );
};

export default ProjectPage;
